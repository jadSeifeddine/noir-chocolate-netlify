  </main>
  <footer class="site-footer">
    <div class="wrap">
      <div class="col">
        <div class="brand">NOI<span>R</span></div>
        <p>Chocolate • Coffee • Cakes.<br />Your go-to sweet spot in Badaro, Beirut.</p>
      </div>
      <div class="col">
        <h4>Explore</h4>
        <a href="/">Home</a>
        <a href="/shop">Full Menu</a>
        <a href="/story">Our Story</a>
        <a href="/contact">Visit &amp; Delivery</a>
        <a href="/cart">Your Bag</a>
      </div>
      <div class="col">
        <h4>Find Us</h4>
        <a href="https://www.instagram.com/noirchocolate.co/" target="_blank" rel="noopener">@noirchocolate.co</a>
        <a href="/contact">Badaro, Beirut</a>
        <a href="/contact">Daily · 9am – 11pm</a>
      </div>
    </div>
    <div class="copyr">© <%= new Date().getFullYear() %> NOIR · Sample website · Crafted with cocoa &amp; care</div>
  </footer>

  <script>
    // Reveal sections as they enter view. Purely additive — everything is
    // already visible without this; it just times the "when."
    (function () {
      var els = document.querySelectorAll('.reveal');
      if (!els.length) return;
      if (!('IntersectionObserver' in window)) {
        els.forEach(function (el) { el.classList.add('is-visible'); });
        return;
      }
      var io = new IntersectionObserver(function (entries) {
        entries.forEach(function (entry) {
          if (entry.isIntersecting) {
            entry.target.classList.add('is-visible');
            io.unobserve(entry.target);
          }
        });
      }, { threshold: 0.05, rootMargin: '0px 0px 120px 0px' });
      els.forEach(function (el) { io.observe(el); });
    })();
  </script>
</body>
</html>
